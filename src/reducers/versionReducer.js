// import { UPDATE_VERSION } from "../actions/types"; // placeholder for imports if needed

const initialState = {
  toolVersion: "1.0",
};

export default function versionReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
